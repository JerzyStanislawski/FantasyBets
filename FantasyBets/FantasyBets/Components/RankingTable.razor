
<div>
    <ul>
        @{
            var iterator = 1;
        }
        @foreach (var user in TotalRank)
        {
            <li class="center ranking-table">
                <a href="/UserBets/@user.Id" class="inline">
                    <p class="width8percent inline text-right">@(iterator++).</p>
                    <p class="width50percent inline">@user.Name</p>
                    <p class="width16percent inline text-right">@user.Points</p>
                </a>
            </li>
        }
    </ul>
</div>

@code {
    [Parameter]
    public IEnumerable<UserPoints> TotalRank { get; set; } = new List<UserPoints>();

    public record UserPoints(string Name, string Id, decimal Points);
}
